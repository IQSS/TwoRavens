import m from 'mithril';

import * as app from '../app';

class Search {
    view(vnode) {
        return m('input#searchvar.form-control[type=text][placeholder=Search variables and labels][style=width: 100%; margin-bottom: 5px]');
    }
}

export default Search;
